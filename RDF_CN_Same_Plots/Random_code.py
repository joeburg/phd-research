#Random code 



def column(matrix, i):
    return [row[i] for row in matrix]


#inputfile = open(filename, 'r')
#for line in inputfile.readlines():
#    tmp = []
#    for element in line[0:-1].split(' '):
#        tmp.append(float(element))
#data.append(tmp)

#data = open(filename, "rU")

#R_list = []
#header = data.readline().rstrip() 

#radius.save("radius.txt")
#rdf.save("rdf.txt")
#coordnum.save("coord_num")

#x = radius(rdf_data)
#y = rdf(rdf_data)


#create an array of points

#create the plot
#plt.plot(x,y)

#plt.savefig(figurename)
